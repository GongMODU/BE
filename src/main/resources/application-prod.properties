# Server
server.port=8080
server.forward-headers-strategy=framework

# PostgreSQL
spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
# spring.jpa.properties.hibernate.format_sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Redis
spring.data.redis.host=${REDIS_HOST}
spring.data.redis.port=6379
spring.data.redis.timeout=3000
spring.data.redis.password=${REDIS_PASSWORD}
spring.cache.type=redis

# Youtube Data API key
youtube.api-key=${YOUTUBE_API_KEY}
youtube.cookies-path=src/main/resources/scripts/youtube_cookies.txt
youtube.channel-ids=UCsJ6RuBiTVWRX156FVbeaGg,UCiHCmazdsMM9j3Jbqx3p7Ig,UC_JXWoZ2DQB98xFiKLPrVEQ,UCwXOKS-z1t9u6Axmm3blXug,UCxfko2YOD6DODYRGzeOPhIQ,UCOB62fKRT7b73X7tRxMuN2g,UCJo6G1u0e_-wS-JQn3T-zEw,UChlv4GSd7OQl3js-jkLOnFA
admin.youtube.key=${ADMIN_YOUTUBE_KEY}
youtube.prod.replace-url=${YOUTUBE_PROD_REPLACE_URL}

# IPO 운영 관리자 API 인증 키 (IpoAdminApiKeyFilter 가 X-IPO-ADMIN-KEY 헤더와 비교)
admin.ipo.key=${ADMIN_IPO_KEY}

# DART
external.dart.base-url=https://opendart.fss.or.kr
external.dart.api-key=${DART_API_KEY}
# KIS
external.kis.base-url=https://openapi.koreainvestment.com:9443
external.kis.app-key=${KIS_APP_KEY}
external.kis.app-secret=${KIS_APP_SECRET}

# JWT
jwt.secret-key=${JWT_SECRET_KEY}
jwt.access-token-expiration=1800000
jwt.refresh-token-expiration=604800000

# Email (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# OAuth2 - Google
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/api/v1/auth/oauth2/callback/google

# OAuth2 - Kakao
spring.security.oauth2.client.registration.kakao.client-id=${KAKAO_CLIENT_ID}
spring.security.oauth2.client.registration.kakao.client-secret=${KAKAO_CLIENT_SECRET}
spring.security.oauth2.client.registration.kakao.client-authentication-method=client_secret_post
spring.security.oauth2.client.registration.kakao.authorization-grant-type=authorization_code
spring.security.oauth2.client.registration.kakao.redirect-uri={baseUrl}/api/v1/auth/oauth2/callback/kakao
spring.security.oauth2.client.registration.kakao.scope=profile_nickname
spring.security.oauth2.client.registration.kakao.client-name=Kakao
spring.security.oauth2.client.provider.kakao.authorization-uri=https://kauth.kakao.com/oauth/authorize
spring.security.oauth2.client.provider.kakao.token-uri=https://kauth.kakao.com/oauth/token
spring.security.oauth2.client.provider.kakao.user-info-uri=https://kapi.kakao.com/v2/user/me
spring.security.oauth2.client.provider.kakao.user-name-attribute=id

# Email Verification
email.verification.expiration=300

# Password Reset
password.reset.expiration=30

# Claude LLM (anthropic)
anthropic.api-key=${ANTHROPIC_API_KEY}
anthropic.model=${ANTHROPIC_MODEL}
anthropic.api-url=${ANTHROPIC_API_URL}
anthropic.max-tokens.detail=8192
anthropic.max-tokens.parser=2048
anthropic.timeout.read=120
# 크레딧 한도 및 모델 단가 (단위: USD / 백만 토큰)
anthropic.credit-limit-usd=25.0
anthropic.price-per-million-input-tokens=3.0
anthropic.price-per-million-output-tokens=15.0

# Frontend URL
app.frontend-url=${FRONTEND_URL}

# 스케줄러 토글 - 기본값 true
# 스케줄러 전체 on/off (토큰 비용 일시 차단)
# false 면 SchedulingConfig 빈이 등록되지 않아 ExternalDataScheduler 의 모든 @Scheduled 메서드가 일절 실행되지 않음
# 발표 기간 일시 OFF (03:00 reparse 가 청약일을 매일 덮어쓰는 현상 차단). 발표 후 true 로 복원.
scheduler.enabled=true

# AI 파이프라인(Claude 호출)만 별도 on/off - 기본 true
# 02:00 / 03:00 / 08:00 의 AI 호출 스케줄러만 skip. 01:00 / 04:00 같은 무료 스케줄러는 영향 없음
# 전체 토글(scheduler.enabled)이 우선하며 false면 이 값은 의미 없음
scheduler.ai-pipelines.enabled=true
